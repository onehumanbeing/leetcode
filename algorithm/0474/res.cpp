#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int findMaxForm(vector<string>& strs, int m, int n) {
        int list_size = strs.size();
        if(list_size==0)return 0;
        int storage[list_size][2];
        
    }
};

int main(){
    return 0;
}