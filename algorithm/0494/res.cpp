#include <iostream>
#include <vector>
using namespace std;

/*
    数组的长度不会超过20，并且数组中的值全为正数。
    初始的数组的和不会超过1000。
    保证返回的最终结果为32位整数。
*/

class Solution {
public:
    int findTargetSumWays(vector<int>& nums, int S) {

    }
};

int main(){
    return 0;
}